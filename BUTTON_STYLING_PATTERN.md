# DEPRECATED

All button styling is handled by the `<Button>` component in `/src/app/components/Button.tsx`.

**Usage:** `import { Button } from "./Button";` then `<Button variant="primary">Label</Button>`

See `/Guidelines.md` > Buttons section and `/guidelines/components.md` > Button Patterns for full docs.
